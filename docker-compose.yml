version: '2'

services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      URL_HOST: https://vm.aleo.org/api
      PROGRAM: aleo_name_service_v0.aleo
    restart: always
    ports:
      - '8000:8000'
